  //String[] top10 = {"US", "JP", "DE", "FR", "HK", "CH", "CN", "SG", "AE", "CA", "NL", "ID", "IT", "EC"};
  //List<String> list = Arrays.asList(top10);
  //if (list.contains(A) && list.contains(A)) {


//int tradeBetween(String importState, String exportState) {
//  // total trade between two countries
//  int totalCount = 0;
//  int puposeCount = 0;
//  float quantityI;
//  float quantityE;

//  for (TableRow row : tradeData.rows()) {

//    if (exporter.equals(exportState) && importer.equals(importState)) {
//      tempCount++;


//      if (quantity == 0.0) { 
//        //println("China traded with " + exporter);
//        //println(row.getString("Exporter"));
//      }
//      println(quantity);
//    }
//    println("Total count is " + tempCount);
//    return tempCount;
//  }
//}